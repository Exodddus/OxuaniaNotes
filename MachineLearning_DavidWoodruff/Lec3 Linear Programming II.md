
# L1 Regression

给定$n$个数据点（$n > 特征数d$），对应 n×d 的矩阵 A 和 n×1 的向量 b，想找一个 d 维向量 x，让 A·x 尽量接近 b。
为了找到尽可能接近的x，定义损失函数 $\Sigma|A_i·x-b_i|$ 。绝对值可以采用以下变换方法转化为线性规划的约束条件：

> 1. 新增n个非负变量$s_i (s_i >= 0)$ ，每个$s_i$对应一个误差的绝对值。
> 2. 加约束 $A_i·x-b_i ≤ s_i$ 且 $-(A_i·x-b_i) ≤ s_i$
> 3. 目标函数化为$\Sigma s_i$ .



